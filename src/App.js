import { BrowserRouter as Router } from 'react-router-dom';

import Routing from './routes/Routing';

const App = () => {
  return (
    <Router>
      <Routing />
    </Router>
  );
};

export default App;
